<!--<Window x:Class="EquipmentSimulator.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EquipmentSimulator.Desktop"
        mc:Ignorable="d"
        Title="Equipment Simulator" Height="450" Width="800">
    <Grid>
        
    </Grid>
</Window>-->
<Window x:Class="EquipmentSimulator.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Equipment Simulator"
        Height="400"
        Width="600">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Equipment Simulator"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding EquipmentId}"
                   FontSize="18"
                   Margin="0,5"/>

        <TextBlock Grid.Row="2"
                   Text="{Binding EquipmentState}"
                   FontSize="18"
                   Margin="0,5"/>

        <TextBlock Grid.Row="3"
                   Text="{Binding ConnectionStatus}"
                   FontSize="18"
                   Margin="0,5"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    Margin="0,20,0,0">

            <Button Content="Connect"
                    Command="{Binding ConnectCommand}"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"/>

            <Button Content="Disconnect"
                    Command="{Binding DisconnectCommand}"
                    Width="100"
                    Height="35"/>

        </StackPanel>

    </Grid>

</Window>